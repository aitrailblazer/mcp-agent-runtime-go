{
  "$schema": "https://modelcontextprotocol.io/schema/2025-03-26/schema.json",
  "promptClass": "tool-invoke",
  "tools": [
    {
      "name": "summarizer",
      "description": "Summarizes input text into a concise summary.",
      "parameters": {
        "type": "object",
        "properties": {
          "text": {
            "type": "string",
            "description": "The text to summarize"
          }
        },
        "required": [
          "text"
        ]
      }
    }
  ]
}